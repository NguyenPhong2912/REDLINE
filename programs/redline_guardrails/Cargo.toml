[package]
name = "redline_guardrails"
version = "0.1.0"
description = "On-chain policy guardrails for REDLINE agents"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "redline_guardrails"

[features]
default = []
cpi = ["no-entrypoint"]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
idl-build = ["anchor-lang/idl-build"]

[dependencies]
anchor-lang = "0.32.1"
